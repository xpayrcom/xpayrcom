# Contributing

Open an issue before proposing a broad behavioral change. Keep pull requests focused, add or update tests, and document any public API impact.

Run the local checks before submitting a pull request:

```bash
npm test
npm run update-profile
git diff --check
```

Do not submit credentials, private keys, seed phrases, production logs, or customer data.

